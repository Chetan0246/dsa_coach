import type { CompactProblem } from './problemHelpers'

/** Phases 1-6: Arrays & Hashing, Two Pointers, Sliding Window, Stack, Binary Search, Linked List. */
export const PROBLEMS_PART1: CompactProblem[] = [
  /* ---------------- Phase 1 — Arrays & Hashing ---------------- */
  {
    t: 'Two Sum',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Easy',
    desc: 'Given an integer array nums and an integer target, return the indices of the two numbers that add up to target. Exactly one valid answer exists, and you may not use the same element twice.',
    sig: [
      'for each value you need to know: have I already seen its partner?',
      'pair / complement question',
      'one pass lookup beats re-scanning the array',
    ],
    brute: 'Check every pair (i, j) with two nested loops until nums[i] + nums[j] == target.',
    bot: 'The second loop only ever asks "does target - nums[i] exist earlier?" — an O(n) scan repeated n times is O(n²).',
    pHint: 'Remember what you have seen while scanning once.',
    oHint: 'Store value -> index in a HashMap; before inserting nums[i], check whether target - nums[i] is already there.',
    java: ['HashMap', 'containsKey', 'arrays as return values'],
    time: 'O(n)',
    space: 'O(n)',
    traps: [
      'checking the map for target - x before inserting x, so an element never pairs with itself',
      'returning values instead of indices',
    ],
    mins: 15,
    ex: [
      { input: 'nums = [2,7,11,15], target = 9', output: '[0,1]', explanation: 'nums[0] + nums[1] == 9.' },
      { input: 'nums = [3,2,4], target = 6', output: '[1,2]' },
    ],
    con: ['2 <= nums.length <= 10^4', 'exactly one valid answer exists'],
    tmpl: `class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> seen = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            int need = target - nums[i];
            if (seen.containsKey(need)) {
                return new int[] { seen.get(need), i };
            }
            seen.put(nums[i], i);
        }
        throw new IllegalArgumentException("no answer");
    }
}`,
    outline:
      'Scan once. For each x, compute need = target - x. If need was seen, return its stored index with i. Otherwise store x -> i and continue.',
    code: `class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> seen = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            int need = target - nums[i];
            if (seen.containsKey(need)) {
                return new int[] { seen.get(need), i };
            }
            seen.put(nums[i], i);
        }
        throw new IllegalArgumentException("no answer");
    }
}`,
    tests: [
      { input: '[2,7,11,15], 9', output: '[0,1]' },
      { input: '[3,2,4], 6', output: '[1,2]' },
    ],
    tags: ['array', 'hashmap'],
  },
  {
    t: 'Contains Duplicate',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Easy',
    desc: 'Given an integer array, return true if any value appears at least twice, and false if every element is distinct.',
    sig: ['duplicate detection', 'membership test per element'],
    brute: 'Compare every pair of elements with nested loops.',
    bot: 'Repeated pair comparison is O(n²) when the only question is "was this value seen before?".',
    pHint: 'A set remembers membership in O(1).',
    oHint: 'Add each element to a HashSet; if add() returns false, you found a duplicate.',
    java: ['HashSet', 'add returning boolean'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['sorting also works (O(n log n)) but forgetting neighbors check', 'using List.contains which is O(n)'],
    mins: 15,
    ex: [
      { input: '[1,2,3,1]', output: 'true' },
      { input: '[1,2,3,4]', output: 'false' },
    ],
    tags: ['array', 'hashset'],
  },
  {
    t: 'Valid Anagram',
    cat: 'Arrays & Hashing',
    pat: 'Frequency Counting',
    d: 'Easy',
    desc: 'Given two strings s and t, return true if t is an anagram of s — same characters with the same frequencies, in any order.',
    sig: ['compare character counts', 'order does not matter'],
    brute: 'Sort both strings and compare the results.',
    bot: 'Sorting is O(n log n) when you only need counts, not order.',
    pHint: 'Count how many times each character appears.',
    oHint: 'Tally s in an int[26], subtract t; all counts must end at zero. One pass each, no sorting.',
    java: ['int[26] as a counter', 'toCharArray'],
    time: 'O(n)',
    space: 'O(1) — fixed 26 letters',
    traps: ['lengths differing must short-circuit to false', 'unicode inputs break the fixed 26-size counter'],
    mins: 15,
    ex: [
      { input: 's = "anagram", t = "nagaram"', output: 'true' },
      { input: 's = "rat", t = "car"', output: 'false' },
    ],
    tags: ['string', 'counting'],
  },
  {
    t: 'Group Anagrams',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'Given an array of strings, group all anagrams together into sublists. Every anagram shares the same multiset of letters.',
    sig: ['grouping by a shared property', 'a canonical key makes anagrams identical'],
    brute: 'For each word, compare sorted letters against every existing group.',
    bot: 'Pairwise comparisons are O(n² · L) — the real question is "which bucket does this word belong to?".',
    pHint: 'Anagrams become identical if you normalize them.',
    oHint: 'Use a HashMap from sorted word (or 26-count key) -> list of words; one pass groups everything.',
    java: ['HashMap of List values', 'char[] to String as a key'],
    time: 'O(n · L log L)',
    space: 'O(n · L)',
    traps: ['sorted-string keys allocate a lot for huge inputs; the count-array key is faster', 'forgetting new ArrayList when a bucket is missing'],
    mins: 20,
    ex: [
      { input: '["eat","tea","tan","ate","nat","bat"]', output: '[["bat"],["nat","tan"],["ate","eat","tea"]]' },
    ],
    tags: ['string', 'hashmap'],
  },
  {
    t: 'Top K Frequent Elements',
    cat: 'Arrays & Hashing',
    pat: 'Heap / Top-K',
    d: 'Medium',
    desc: 'Given an integer array and an integer k, return the k most frequent elements.',
    sig: ['most frequent K', 'rank by a count you must compute first'],
    brute: 'Count frequencies, then sort all distinct values by count descending and take the first k.',
    bot: 'Sorting all n distinct values is O(n log n) when you only need k of them.',
    pHint: 'You only ever need the current best few while scanning.',
    oHint: 'Count with a HashMap, then keep a min-heap of size k (or bucket-sort by frequency for O(n)).',
    java: ['HashMap counting', 'PriorityQueue with comparator', 'bucket sort variant'],
    time: 'O(n log k)',
    space: 'O(n)',
    traps: ['using a max-heap and keeping all n elements', 'comparator sign flipped, giving k least frequent'],
    mins: 20,
    ex: [
      { input: 'nums = [1,1,1,2,2,3], k = 2', output: '[1,2]' },
      { input: 'nums = [1], k = 1', output: '[1]' },
    ],
    tags: ['hashmap', 'heap'],
  },
  {
    t: 'Product of Array Except Self',
    cat: 'Arrays & Hashing',
    pat: 'Prefix Sum',
    d: 'Medium',
    desc: 'Return an array answer where answer[i] equals the product of every element except nums[i]. Division is not allowed and O(n) is required.',
    sig: ['product of "everything else"', 'two independent directions combine'],
    brute: 'For each index multiply all other elements — O(n²).',
    bot: 'Each answer recomputes overlapping products; prefix products in two directions remove the repetition.',
    pHint: 'What is already multiplied before index i, and after it?',
    oHint: 'Store prefix products left-to-right, then sweep right-to-left multiplying a running suffix product.',
    java: ['prefix products', 'in-place output array'],
    time: 'O(n)',
    space: 'O(1) extra (output excluded)',
    traps: ['forgetting the running suffix starts at 1', 'attempting division with zeros present'],
    mins: 20,
    ex: [
      { input: '[1,2,3,4]', output: '[24,12,8,6]' },
    ],
    tags: ['array', 'prefix'],
  },
  {
    t: 'Valid Sudoku',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'Validate a partially filled 9x9 board: digits 1-9 must not repeat in any row, column, or 3x3 box. Empty cells are "." and are ignored.',
    sig: ['three membership domains per cell', 'box index math'],
    brute: 'For each digit, scan its row, column, and box for duplicates.',
    bot: 'Same cells are re-visited many times; one pass with three seen-sets is enough.',
    pHint: 'Each cell belongs to exactly one row, one column, and one box — track all three.',
    oHint: 'Iterate once; use sets keyed "row-i-val", "col-j-val", "box-(i/3*3+j/3)-val", or 27 boolean arrays.',
    java: ['HashSet', 'integer box-index arithmetic'],
    time: 'O(81) — effectively O(1)',
    space: 'O(1) — bounded by 81 cells',
    traps: ['box index must be (r / 3) * 3 + c / 3, not r / 3 + c / 3', 'checking validity only for filled cells'],
    mins: 25,
    ex: [
      { input: 'standard valid board with "." empties', output: 'true' },
      { input: 'same board but top-left digit also appears in its 3x3 box', output: 'false' },
    ],
    tags: ['matrix', 'hashset'],
  },
  {
    t: 'Encode and Decode Strings',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'Design an algorithm that encodes a list of strings into one string, and decodes it back into the original list with no information loss.',
    sig: ['serialization design', 'delimiters must survive arbitrary content'],
    brute: 'Join with a chosen separator character.',
    bot: 'Any single separator can appear inside a string, corrupting decode.',
    pHint: 'Send the length of each piece alongside the data.',
    oHint: 'Prefix every string with its length and a marker ("4#code"); decode reads the number, then slices exactly that many chars.',
    java: ['StringBuilder', 'indexOf', 'substring'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['forgetting strings may contain the delimiter itself', 'decoding before validating the length prefix'],
    mins: 20,
    ex: [
      { input: '["lint","code","love"]', output: 'encoded then decoded equals the original list' },
      { input: '["we","say",":]"]', output: 'decodes back exactly' },
    ],
    tags: ['string', 'design'],
  },
  {
    t: 'Longest Consecutive Sequence',
    cat: 'Arrays & Hashing',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'Given an unsorted integer array, return the length of the longest sequence of consecutive integers (values, not positions). It must run in O(n).',
    sig: ['membership questions on values', 'O(n) with unsorted input forbids sorting'],
    brute: 'Sort and count consecutive runs — O(n log n).',
    bot: 'Sorting orders data whose order does not matter; only membership matters.',
    pHint: 'Only sequence starts need counting — a start has no left neighbor.',
    oHint: 'Put all values in a HashSet; for each x with x-1 absent, walk x, x+1, ... counting run length.',
    java: ['HashSet', 'the "only start runs" insight'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['walking from every number, making it O(n²) worst case', 'forgetting the x-1 membership gate'],
    mins: 25,
    ex: [
      { input: '[100,4,200,1,3,2]', output: '4', explanation: 'The run 1,2,3,4.' },
      { input: '[0,3,7,2,5,8,4,6,0,1]', output: '9' },
    ],
    tags: ['array', 'hashset'],
  },

  /* ---------------- Phase 2 — Two Pointers ---------------- */
  {
    t: 'Valid Palindrome',
    cat: 'Two Pointers',
    pat: 'Two Pointers',
    d: 'Easy',
    desc: 'Given a string, return true if it reads the same forwards and backwards after lowercasing and removing all non-alphanumeric characters.',
    sig: ['symmetry between both ends', 'string can be inspected in place'],
    brute: 'Build a cleaned copy of the string, then reverse it and compare.',
    bot: 'Extra O(n) copies when two indices can verify symmetry directly.',
    pHint: 'Compare characters from both ends moving inward.',
    oHint: 'One left pointer, one right pointer; skip non-alphanumerics, lowercase, compare, meet in the middle.',
    java: ['Character.isLetterOrDigit', 'Character.toLowerCase', 'two index variables'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['forgetting digits count as alphanumeric', 'loop condition left < right vs <='],
    mins: 15,
    ex: [
      { input: '"A man, a plan, a canal: Panama"', output: 'true' },
      { input: '"race a car"', output: 'false' },
    ],
    tags: ['string', 'pointers'],
  },
  {
    t: 'Two Sum II — Input Array Is Sorted',
    cat: 'Two Pointers',
    pat: 'Two Pointers',
    d: 'Medium',
    desc: 'Given a sorted (non-decreasing) array and a target, return the 1-based indices of the two numbers that add up to the target.',
    sig: ['sorted array', 'pair relationship', 'opposite ends'],
    brute: 'Try every pair — O(n²); the sorted order is unused.',
    bot: 'When the sum is too big or too small, whole halves of possibilities are ignored by the brute force.',
    pHint: 'Move one end inward based on how the current sum compares to the target.',
    oHint: 'Left at start, right at end: sum too small -> left++, too big -> right--, equal -> answer.',
    java: ['while loop with two indices', '1-based output'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['moving both pointers at once', 'forgetting the 1-based return'],
    mins: 15,
    ex: [
      { input: '[2,7,11,15], target = 9', output: '[1,2]' },
    ],
    tags: ['array', 'pointers'],
  },
  {
    t: '3Sum',
    cat: 'Two Pointers',
    pat: 'Two Pointers',
    d: 'Medium',
    desc: 'Return all unique triplets in the array that sum to zero. The answer must not contain duplicate triplets.',
    sig: ['pair search inside a sorted array', 'de-duplication required'],
    brute: 'Three nested loops over i < j < k checking the sum, plus a set for uniqueness.',
    bot: 'The inner pair search can be O(n) with two pointers once the array is sorted.',
    pHint: 'Fix one number, then search for a two-sum in what remains.',
    oHint: 'Sort; for each i run left/right two-sum on i+1..end; skip duplicates at all three positions.',
    java: ['Arrays.sort', 'two pointers', 'duplicate skipping'],
    time: 'O(n²)',
    space: 'O(1) extra (ignoring sort)',
    traps: ['duplicate i values and duplicate inner values must be skipped', 'long overflow on extreme values — use long sums if constrained'],
    mins: 25,
    ex: [
      { input: '[-1,0,1,2,-1,-4]', output: '[[-1,-1,2],[-1,0,1]]' },
    ],
    tags: ['array', 'pointers', 'sorting'],
  },
  {
    t: 'Container With Most Water',
    cat: 'Two Pointers',
    pat: 'Two Pointers',
    d: 'Medium',
    desc: 'Each index holds a vertical line of that height. Pick two lines that, with the x-axis, bound the largest container of water. Return that maximum area.',
    sig: ['two ends of an array', 'area governed by the smaller height'],
    brute: 'Evaluate area for every pair — O(n²).',
    bot: 'When both pointers move inward arbitrarily, better answers get skipped.',
    pHint: 'Which pointer can move without possibly losing the best answer?',
    oHint: 'Always move the pointer at the shorter line — the shorter side caps the area, so keeping it cannot help.',
    java: ['greedy pointer movement', 'Math.min / Math.max'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['moving the taller pointer instead', 'area uses distance, not index difference + 1'],
    mins: 20,
    ex: [
      { input: '[1,8,6,2,5,4,8,3,7]', output: '49', explanation: 'Lines at indices 1 and 8, area = min(8,7) * 7.' },
    ],
    tags: ['array', 'greedy'],
  },
  {
    t: 'Trapping Rain Water',
    cat: 'Two Pointers',
    pat: 'Two Pointers',
    d: 'Hard',
    desc: 'Given elevation bars, compute how much water can be trapped between them after it rains.',
    sig: ['water above bar i = min(maxLeft, maxRight) - height[i]', 'two boundary scans'],
    brute: 'For each bar scan left and right for the maxima — O(n²).',
    bot: 'Prefix/suffix max arrays already fix it in O(n); two pointers reach O(1) space.',
    pHint: 'Water level at each bar depends only on the taller wall on each side.',
    oHint: 'Two pointers with running leftMax/rightMax; advance the side with the smaller max and settle its water.',
    java: ['two pointers', 'running maxima'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['updating the wrong pointer after computing water', 'min vs max on the two maxima'],
    mins: 30,
    ex: [
      { input: '[0,1,0,2,1,0,1,3,2,1,2,1]', output: '6' },
    ],
    tags: ['array', 'pointers'],
  },

  /* ---------------- Phase 3 — Sliding Window ---------------- */
  {
    t: 'Best Time to Buy and Sell Stock',
    cat: 'Sliding Window',
    pat: 'Sliding Window',
    d: 'Easy',
    desc: 'You see daily prices. Choose one day to buy and a later day to sell to maximize profit; return 0 when no profit is possible.',
    sig: ['best pair (i < j) with values interacting', 'running minimum suffices'],
    brute: 'Compare every buy/sell pair — O(n²).',
    bot: 'Each pair recomputes what a running minimum already knows.',
    pHint: 'Track the cheapest price seen so far while walking forward.',
    oHint: 'One pass: best = max(best, price - minSoFar), then minSoFar = min(minSoFar, price).',
    java: ['single pass', 'Math.min / Math.max'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['selling before buying', 'initializing minSoFar to 0 instead of the first price / MAX_VALUE'],
    mins: 15,
    ex: [
      { input: '[7,1,5,3,6,4]', output: '5', explanation: 'Buy at 1, sell at 6.' },
      { input: '[7,6,4,3,1]', output: '0' },
    ],
    tags: ['array', 'window'],
  },
  {
    t: 'Longest Substring Without Repeating Characters',
    cat: 'Sliding Window',
    pat: 'Sliding Window',
    d: 'Medium',
    desc: 'Find the length of the longest substring of s that contains no repeated characters.',
    sig: ['contiguous substring', 'longest valid range', 'condition changes as window moves'],
    brute: 'Check every substring for uniqueness — O(n³) naively.',
    bot: 'Overlapping substrings are re-checked; window state can be repaired instead of recomputed.',
    pHint: 'Keep a window that is always duplicate-free.',
    oHint: 'Expand right; when a duplicate enters, shrink from the left until the window is valid again; track max size.',
    java: ['HashSet or last-index map', 'two indices'],
    time: 'O(n)',
    space: 'O(min(n, alphabet))',
    traps: ['moving left by 1 instead of jumping past the duplicate with an index map', 'updating best before shrinking'],
    mins: 20,
    ex: [
      { input: '"abcabcbb"', output: '3', explanation: '"abc".' },
      { input: '"bbbbb"', output: '1' },
      { input: '"pwwkew"', output: '3' },
    ],
    tags: ['string', 'window'],
  },
  {
    t: 'Longest Repeating Character Replacement',
    cat: 'Sliding Window',
    pat: 'Sliding Window',
    d: 'Medium',
    desc: 'You may change at most k characters. Return the length of the longest substring made of one repeated character after changes.',
    sig: ['window validity measured by (length - maxFrequency) <= k', 'frequency inside a moving range'],
    brute: 'Try every substring, count its most frequent letter, keep valid ones.',
    bot: 'Counts for each window are recomputed from scratch.',
    pHint: 'A window is valid when the non-dominant characters fit inside k.',
    oHint: 'Slide with a count map; shrink while window length - maxCount > k; track best length.',
    java: ['int[26] counts', 'two indices'],
    time: 'O(n)',
    space: 'O(26)',
    traps: ['recomputing maxCount when shrinking (just keep the max ever seen)', 'forgetting the window only needs to grow or stay, never shrink fully'],
    mins: 25,
    ex: [
      { input: '"ABAB", k = 2', output: '4' },
      { input: '"AABABBA", k = 1', output: '4' },
    ],
    tags: ['string', 'window'],
  },
  {
    t: 'Permutation in String',
    cat: 'Sliding Window',
    pat: 'Sliding Window',
    d: 'Medium',
    desc: 'Return true if s2 contains any permutation of s1 as a contiguous substring.',
    sig: ['fixed-length window', 'compare frequency signatures'],
    brute: 'Sort every length-m substring of s2 and compare with sorted s1.',
    bot: 'Each window is recounted from zero.',
    pHint: 'The window length never changes — slide and update counts incrementally.',
    oHint: 'Count s1; slide a window of length m over s2 adding/removing one char per step; compare counts.',
    java: ['int[26] arrays', 'matches counter technique'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['comparing full arrays each step is fine (26) but rebuilding them is not', 'window size off by one'],
    mins: 25,
    ex: [
      { input: 's1 = "ab", s2 = "eidbaooo"', output: 'true', explanation: '"ba" is a permutation and is inside s2.' },
      { input: 's1 = "ab", s2 = "eidboaoo"', output: 'false' },
    ],
    tags: ['string', 'window'],
  },
  {
    t: 'Minimum Window Substring',
    cat: 'Sliding Window',
    pat: 'Sliding Window',
    d: 'Hard',
    desc: 'Find the shortest substring of s that contains every character of t (with multiplicity). Return "" when none exists.',
    sig: ['shortest valid window', 'need/have counting'],
    brute: 'Check every substring for covering t — O(n² · alphabet).',
    bot: 'Validity can be maintained with a single "formed" counter instead of rescanning.',
    pHint: 'Expand until the window has everything, then shrink while it stays valid.',
    oHint: 'Need-map for t; expand right updating "formed"; when formed == need, contract left recording the best window.',
    java: ['HashMap counts', 'formed counter'],
    time: 'O(n + m)',
    space: 'O(alphabet)',
    traps: ['shrinking before the window is complete', 'multiplicity: two "a" in t require two "a" in the window'],
    mins: 30,
    ex: [
      { input: 's = "ADOBECODEBANC", t = "ABC"', output: '"BANC"' },
      { input: 's = "a", t = "aa"', output: '""' },
    ],
    tags: ['string', 'window'],
  },
  {
    t: 'Sliding Window Maximum',
    cat: 'Sliding Window',
    pat: 'Monotonic Stack',
    d: 'Hard',
    desc: 'Return the maximum of every contiguous window of size k in the array, in order, in O(n).',
    sig: ['max of moving range', 'recomputing max each slide is the slow part'],
    brute: 'Scan each window for its max — O(n·k).',
    bot: 'The max is recomputed even though most elements carry over.',
    pHint: 'Keep only indices that can still be a future maximum.',
    oHint: 'Monotonic decreasing deque of indices; front is the current max; evict expired front, pop smaller backs.',
    java: ['ArrayDeque as monotonic deque', 'index-based eviction'],
    time: 'O(n)',
    space: 'O(k)',
    traps: ['storing values not indices (cannot detect expiry)', 'keeping equal elements instead of popping them'],
    mins: 30,
    ex: [
      { input: '[1,3,-1,-3,5,3,6,7], k = 3', output: '[3,3,5,5,6,7]' },
    ],
    tags: ['array', 'deque'],
  },

  /* ---------------- Phase 4 — Stack ---------------- */
  {
    t: 'Valid Parentheses',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Easy',
    desc: 'Given a string of ()[]{}, return true if every bracket is closed by the same type in the correct order.',
    sig: ['most recently opened must close first', 'LIFO behavior'],
    brute: 'Repeatedly delete adjacent pairs until none remain, then check emptiness.',
    bot: 'Each deletion pass is O(n) and may run O(n) times.',
    pHint: 'Which structure pairs an opening with its matching close?',
    oHint: 'Push openers; on a closer, the top must be its opener — pop or fail; end requires an empty stack.',
    java: ['ArrayDeque as stack', 'a switch or map of pairs'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['forgetting the empty-at-end check', 'closing bracket arriving with an empty stack'],
    mins: 15,
    ex: [
      { input: '"()[]{}"', output: 'true' },
      { input: '"([)]"', output: 'false' },
    ],
    tags: ['string', 'stack'],
  },
  {
    t: 'Min Stack',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Medium',
    desc: 'Design a stack supporting push, pop, top, and getMin — each in O(1) time.',
    sig: ['design question', 'min must survive pops'],
    brute: 'Scan the whole stack on getMin.',
    bot: 'getMin becomes O(n); the spec demands O(1).',
    pHint: 'Remember the minimum as of every pushed value.',
    oHint: 'Keep a second stack of minimums: push x only if it is <= current min; pop in lockstep with the main stack.',
    java: ['two ArrayDeques', 'pair stack alternative'],
    time: 'O(1) per op',
    space: 'O(n)',
    traps: ['popping the min stack only when equal (must use <= to survive duplicates)', 'peeking empty stacks'],
    mins: 20,
    ex: [
      { input: 'push(-2), push(0), push(-3), getMin(), pop(), top(), getMin()', output: '-3, 0, -2' },
    ],
    tags: ['design', 'stack'],
  },
  {
    t: 'Evaluate Reverse Polish Notation',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Medium',
    desc: 'Given an array of tokens forming a Reverse Polish expression, return its value. Operators are +, -, *, /; division truncates toward zero.',
    sig: ['operands wait for operators', 'LIFO evaluation'],
    brute: 'Repeatedly find "a b op" triples and reduce the array.',
    bot: 'Array reduction is O(n²); a stack evaluates in one pass.',
    pHint: 'Operands are consumed in reverse order of appearance.',
    oHint: 'Push numbers; on an operator pop two operands (careful with order), apply, push the result.',
    java: ['ArrayDeque', 'Integer.parseInt', 'truncation toward zero'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['operand order for - and / (second pop is the left operand)', 'Java % keeps the dividend sign'],
    mins: 20,
    ex: [
      { input: '["2","1","+","3","*"]', output: '9', explanation: '(2 + 1) * 3.' },
      { input: '["4","13","5","/","+"]', output: '6' },
    ],
    tags: ['stack', 'math'],
  },
  {
    t: 'Generate Parentheses',
    cat: 'Stack',
    pat: 'Backtracking',
    d: 'Medium',
    desc: 'Given n pairs, produce all strings of well-formed parentheses.',
    sig: ['build candidates incrementally', 'constraint: closes may never exceed opens'],
    brute: 'Generate all 2^(2n) strings and filter by validity.',
    bot: 'Most candidates are invalid; pruning at construction time avoids them.',
    pHint: 'Add "(" while opens remain, add ")" only when it closes an open one.',
    oHint: 'Backtrack with (open, close) counters: recurse adding "(" if open < n, and ")" if close < open.',
    java: ['StringBuilder backtracking', 'recursion counters'],
    time: 'O(4^n / sqrt(n)) — Catalan output size',
    space: 'O(n) recursion',
    traps: ['allowing close > open', 'reusing one StringBuilder without undoing appends'],
    mins: 25,
    ex: [
      { input: 'n = 3', output: '["((()))","(()())","(())()","()(())","()()()"]' },
    ],
    tags: ['backtracking', 'string'],
  },
  {
    t: 'Daily Temperatures',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Medium',
    desc: 'For each day, return how many days until a warmer temperature. If none is coming, answer 0.',
    sig: ['next greater element', 'backward waiting for an answer'],
    brute: 'For each day scan forward until a warmer day — O(n²).',
    bot: 'Cold days get rescanned repeatedly; a monotonic stack answers them once.',
    pHint: 'Which days are still waiting for their warmer day?',
    oHint: 'Stack of indices with decreasing temps; when a warmer day arrives, pop and settle those days.',
    java: ['ArrayDeque of indices', 'answer array'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['strictly greater vs >= comparison', 'leftover stack entries must stay 0'],
    mins: 20,
    ex: [
      { input: '[73,74,75,71,69,72,76,73]', output: '[1,1,4,2,1,1,0,0]' },
    ],
    tags: ['array', 'stack'],
  },
  {
    t: 'Car Fleet',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Medium',
    desc: 'Cars share one road toward a target at given speeds; a faster car behind a slower one becomes one fleet. Return the number of fleets that reach the target.',
    sig: ['compare arrival times from the front', 'stack of leading times'],
    brute: 'Simulate positions each second.',
    bot: 'Simulation is unnecessary — arrival times decide everything.',
    pHint: 'Sort by starting position; when does a car catch the fleet ahead?',
    oHint: 'Compute arrival times sorted by position; count a new fleet when time exceeds the current leader stack top.',
    java: ['index sorting by position', 'stack of times'],
    time: 'O(n log n)',
    space: 'O(n)',
    traps: ['sorting by position descending vs ascending flips comparisons', 'tie in arrival time joins the same fleet'],
    mins: 25,
    ex: [
      { input: 'target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3]', output: '3' },
    ],
    tags: ['array', 'sorting'],
  },
  {
    t: 'Largest Rectangle in Histogram',
    cat: 'Stack',
    pat: 'Monotonic Stack',
    d: 'Hard',
    desc: 'Each bar has width 1; find the largest rectangle that fits entirely under the histogram.',
    sig: ['for each bar: how far can it extend?', 'monotonic heights'],
    brute: 'For each bar expand left/right while neighbors are taller — O(n²).',
    bot: 'Extension limits are recomputed per bar; a monotonic stack settles each bar once.',
    pHint: 'When a shorter bar arrives, taller bars can never extend further.',
    oHint: 'Increasing stack of indices; on a smaller bar pop and compute area with the popped height and current width; flush at the end.',
    java: ['ArrayDeque of indices', 'width computation with left boundary'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['equal heights popping order', 'width formula needs the new stack top after popping'],
    mins: 30,
    ex: [
      { input: '[2,1,5,6,2,3]', output: '10' },
    ],
    tags: ['array', 'stack'],
  },

  /* ---------------- Phase 5 — Binary Search ---------------- */
  {
    t: 'Binary Search',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Easy',
    desc: 'Given a sorted array, return the index of target or -1. Write it in O(log n).',
    sig: ['sorted data', 'halve the search space'],
    brute: 'Linear scan — O(n), ignoring sortedness.',
    bot: 'Each comparison discards nothing; sorted order allows discarding half.',
    pHint: 'Compare with the middle and throw one half away.',
    oHint: 'lo=0, hi=n-1; loop while lo <= hi; mid = lo + (hi-lo)/2; move one boundary past mid each step.',
    java: ['while loop', 'overflow-safe mid'],
    time: 'O(log n)',
    space: 'O(1)',
    traps: ['infinite loop from updating the wrong boundary', '(lo+hi)/2 overflow in fixed-width languages'],
    mins: 15,
    ex: [
      { input: '[-1,0,3,5,9,12], target = 9', output: '4' },
      { input: '[-1,0,3,5,9,12], target = 2', output: '-1' },
    ],
    tags: ['array', 'search'],
  },
  {
    t: 'Search a 2D Matrix',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Medium',
    desc: 'Each matrix row is sorted and each row starts greater than the previous row ends. Decide whether target exists in O(log(m·n)).',
    sig: ['flattened sorted order', 'one monotonic sequence'],
    brute: 'Two binary searches: rows then columns — fine, but the matrix is one list.',
    bot: 'Two-phase search misses that the matrix is a single sorted stream.',
    pHint: 'Index the matrix as one array of length m*n.',
    oHint: 'Binary search over 0..m*n-1; convert idx -> (idx / n, idx % n).',
    java: ['index arithmetic', 'binary search'],
    time: 'O(log(m·n))',
    space: 'O(1)',
    traps: ['row/col conversion rounding', 'empty matrix edge case'],
    mins: 20,
    ex: [
      { input: '[[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3', output: 'true' },
      { input: 'same matrix, target = 13', output: 'false' },
    ],
    tags: ['matrix', 'search'],
  },
  {
    t: 'Koko Eating Bananas',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Medium',
    desc: 'Koko eats piles at k bananas/hour. Find the minimum integer k that lets her finish all piles within h hours (each pile takes ceil(pile / k) hours).',
    sig: ['minimize the maximum speed', 'monotonic feasibility'],
    brute: 'Try k = 1, 2, 3, ... until feasible.',
    bot: 'Feasibility is monotonic in k, so scanning linearly wastes work.',
    pHint: 'Feasible(k) is true, true, ..., true, false, false. Search the boundary.',
    oHint: 'Binary search k between 1 and max(piles); feasibility = sum of ceil(pile/k) <= h.',
    java: ['long sums', 'custom feasibility function'],
    time: 'O(n log maxPile)',
    space: 'O(1)',
    traps: ['integer ceil with (pile + k - 1) / k', 'hour sum overflowing int'],
    mins: 25,
    ex: [
      { input: 'piles = [3,6,7,11], h = 8', output: '4' },
    ],
    tags: ['binary search'],
  },
  {
    t: 'Find Minimum in Rotated Sorted Array',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Medium',
    desc: 'A sorted array was rotated between 1 and n times. Find its minimum element in O(log n).',
    sig: ['two sorted segments', 'compare against the right end'],
    brute: 'Linear scan for the minimum — O(n).',
    bot: 'Sortedness (even rotated) allows halving.',
    pHint: 'The minimum sits where the rotation happens.',
    oHint: 'Compare mid to the rightmost element: mid > right means the minimum is right of mid, else it is at mid or left.',
    java: ['binary search on a condition', 'no target to compare, use boundaries'],
    time: 'O(log n)',
    space: 'O(1)',
    traps: ['comparing to nums[0] instead of nums[hi]', 'moving both boundaries and skipping the answer'],
    mins: 20,
    ex: [
      { input: '[3,4,5,1,2]', output: '1' },
      { input: '[4,5,6,7,0,1,2]', output: '0' },
    ],
    tags: ['array', 'search'],
  },
  {
    t: 'Search in Rotated Sorted Array',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Medium',
    desc: 'A sorted array is rotated at an unknown pivot. Find target in O(log n) or return -1. Values are distinct.',
    sig: ['one half is always sorted', 'boundary reasoning'],
    brute: 'Linear scan.',
    bot: 'The rotation still leaves a sorted half every step.',
    pHint: 'First decide which half is sorted, then ask whether target lies inside it.',
    oHint: 'At each mid, check if nums[lo..mid] is sorted; target in that range -> shrink right, else left; mirrored otherwise.',
    java: ['nested conditionals in binary search'],
    time: 'O(log n)',
    space: 'O(1)',
    traps: ['distinctness is what makes the decisions reliable', 'using >= vs > on the sorted-half test'],
    mins: 25,
    ex: [
      { input: '[4,5,6,7,0,1,2], target = 0', output: '4' },
      { input: '[4,5,6,7,0,1,2], target = 3', output: '-1' },
    ],
    tags: ['array', 'search'],
  },
  {
    t: 'Time Based Key-Value Store',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Medium',
    desc: 'Design a store with set(key, value, timestamp) and get(key, timestamp) returning the value with the largest timestamp <= the given one. Timestamps only grow per key.',
    sig: ['search on a sorted timeline per key', 'boundary <= query'],
    brute: 'Store all (time, value) pairs per key and scan on get.',
    bot: 'Timestamps are strictly increasing per key — a sorted list invites binary search.',
    pHint: 'Per key, the recorded timestamps form a sorted array.',
    oHint: 'Append to a per-key list; get binary searches for the rightmost timestamp <= query.',
    java: ['HashMap of lists', 'custom binary search or Collections.binarySearch'],
    time: 'O(log n) get, O(1) set',
    space: 'O(n)',
    traps: ['rightmost <= not exact match', 'missing key or earlier-than-first query must return ""'],
    mins: 25,
    ex: [
      { input: 'set("a","bar",1); set("a","baz",3); get("a",2)', output: '"bar"' },
    ],
    tags: ['design', 'search'],
  },
  {
    t: 'Median of Two Sorted Arrays',
    cat: 'Binary Search',
    pat: 'Binary Search',
    d: 'Hard',
    desc: 'Two sorted arrays of sizes m and n. Return the median in O(log(m+n)) without merging.',
    sig: ['partition search', 'binary search on the smaller array'],
    brute: 'Merge and pick the middle — O(m+n), which the constraints forbid.',
    bot: 'Only a partition matters: left parts combined must hold half the elements.',
    pHint: 'Binary search the cut in the smaller array; the other cut is forced.',
    oHint: 'Partition both so maxLeftA <= minRightB and maxLeftB <= minRightA; adjust cut by comparison; average for even totals.',
    java: ['boundary binary search', 'sentinel infinities'],
    time: 'O(log(min(m,n)))',
    space: 'O(1)',
    traps: ['binary searching the larger array', 'cut edge cases handled by ±infinity sentinels'],
    mins: 35,
    ex: [
      { input: 'a = [1,3], b = [2]', output: '2.0' },
      { input: 'a = [1,2], b = [3,4]', output: '2.5' },
    ],
    tags: ['array', 'search'],
  },

  /* ---------------- Phase 6 — Linked List ---------------- */
  {
    t: 'Reverse Linked List',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Easy',
    desc: 'Reverse a singly linked list and return the new head. Do it iteratively and recursively.',
    sig: ['pointer surgery one node at a time', 'O(1) space preferred'],
    brute: 'Copy values into an array, reverse, rebuild the list.',
    bot: 'Extra O(n) memory for values when the nodes themselves can be relinked.',
    pHint: 'Keep three references: previous, current, next.',
    oHint: 'Iteratively flip each next pointer to point backward while walking forward; return the last node.',
    java: ['null checks', 'three temp references'],
    time: 'O(n)',
    space: 'O(1) iterative',
    traps: ['losing the rest of the list before relinking', 'returning the wrong final head'],
    mins: 15,
    ex: [
      { input: '1 -> 2 -> 3 -> 4 -> 5', output: '5 -> 4 -> 3 -> 2 -> 1' },
    ],
    tags: ['linked list'],
  },
  {
    t: 'Merge Two Sorted Lists',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Easy',
    desc: 'Merge two sorted linked lists into one sorted list by splicing existing nodes.',
    sig: ['two sorted streams', 'front insert pattern'],
    brute: 'Collect all values, sort, rebuild.',
    bot: 'Values are already ordered; relinking nodes avoids any sorting.',
    pHint: 'A dummy head removes all first-node special cases.',
    oHint: 'Walk both lists with a tail pointer; attach the smaller node and advance that list; attach leftovers at the end.',
    java: ['dummy node', 'while with two cursors'],
    time: 'O(n + m)',
    space: 'O(1)',
    traps: ['forgetting leftover tails', 'not returning dummy.next'],
    mins: 15,
    ex: [
      { input: '1 -> 2 -> 4, 1 -> 3 -> 4', output: '1 -> 1 -> 2 -> 3 -> 4 -> 4' },
    ],
    tags: ['linked list'],
  },
  {
    t: 'Linked List Cycle',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Easy',
    desc: 'Return true if some node in the linked list is reachable again by following next pointers — i.e., the list contains a cycle.',
    sig: ['cycle detection', 'O(1) memory requested'],
    brute: 'Store visited nodes in a HashSet until null or a repeat.',
    bot: 'O(n) memory; two speeds detect the same thing with O(1).',
    pHint: 'Two runners on a circular track must meet.',
    oHint: 'slow moves 1, fast moves 2; if they ever meet there is a cycle; fast hitting null means none.',
    java: ['two references', 'null-check ordering'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['checking fast.next before fast', 'comparing values instead of references'],
    mins: 15,
    ex: [
      { input: '1 -> 2 -> 3 -> 4 with 4.next = 2', output: 'true' },
      { input: '1 -> 2', output: 'false' },
    ],
    tags: ['linked list', 'pointers'],
  },
  {
    t: 'Reorder List',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Medium',
    desc: 'Reorder L0 -> L1 -> ... -> Ln into L0 -> Ln -> L1 -> Ln-1 -> ... in place.',
    sig: ['middle of the list', 'reverse a segment'],
    brute: 'Store nodes in a list and weave by index.',
    bot: 'O(n) extra space; the operations (find middle, reverse, merge) are all O(1)-space primitives.',
    pHint: 'Split the list in half, reverse the second half, zip them together.',
    oHint: 'Fast/slow finds the middle; reverse the back half; merge alternating nodes from both halves.',
    java: ['three classic list techniques chained'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['forgetting to cut the first half before reversing', 'infinite loop from a stray next pointer'],
    mins: 25,
    ex: [
      { input: '1 -> 2 -> 3 -> 4', output: '1 -> 4 -> 2 -> 3' },
    ],
    tags: ['linked list'],
  },
  {
    t: 'Remove Nth Node From End of List',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Medium',
    desc: 'Delete the n-th node counting from the end in one pass.',
    sig: ['two pointers with a gap', 'dummy head for edge cases'],
    brute: 'Count length L, then delete node L - n + 1 in a second pass.',
    bot: 'Two passes; a leading pointer creates the gap in one.',
    pHint: 'Keep two pointers n+1 apart, then move both to the end.',
    oHint: 'Advance fast n+1 times from a dummy head; walk both until fast is null; slow.next is the victim.',
    java: ['dummy node', 'gap technique'],
    time: 'O(L)',
    space: 'O(1)',
    traps: ['deleting the head without a dummy', 'off-by-one in the gap'],
    mins: 20,
    ex: [
      { input: '1 -> 2 -> 3 -> 4 -> 5, n = 2', output: '1 -> 2 -> 3 -> 5' },
    ],
    tags: ['linked list', 'pointers'],
  },
  {
    t: 'Copy List With Random Pointer',
    cat: 'Linked List',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'A list node has next and random pointers. Deep-copy the list so random pointers point at the corresponding copied nodes.',
    sig: ['map old node -> new node', 'two-phase copying'],
    brute: 'Two passes with a HashMap: create copies, then wire next/random through the map.',
    bot: 'The HashMap is fine — O(1) space is possible with interleaving, but the map approach is the clean O(n).',
    pHint: 'First create all nodes, then connect them.',
    oHint: 'Pass 1: copy each node into a map keyed by the original. Pass 2: set copy.next and copy.random from the map.',
    java: ['HashMap<Node, Node>', 'two passes'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['wiring random before all copies exist', 'null random pointers'],
    mins: 25,
    ex: [
      { input: '[[7,null],[13,0],[11,4],[10,2],[1,0]]', output: 'a deep copy with identical structure' },
    ],
    tags: ['linked list', 'hashmap'],
  },
  {
    t: 'Add Two Numbers',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Medium',
    desc: 'Two non-negative integers are stored as reversed digit lists. Add them and return the sum in the same format.',
    sig: ['digit-by-digit with carry', 'unequal lengths'],
    brute: 'Convert both lists to numbers, add, convert back — overflows for long inputs.',
    bot: 'Arbitrary precision is required; simulate schoolbook addition instead.',
    pHint: 'Process both lists and a carry together.',
    oHint: 'Walk both lists: digit sum + carry; append sum % 10; carry = sum / 10; finish with a leftover carry node.',
    java: ['dummy head', 'carry arithmetic'],
    time: 'O(max(m,n))',
    space: 'O(max(m,n)) output',
    traps: ['dropping the final carry', 'lists of different lengths'],
    mins: 20,
    ex: [
      { input: '2 -> 4 -> 3 (342), 5 -> 6 -> 4 (465)', output: '7 -> 0 -> 8 (807)' },
    ],
    tags: ['linked list', 'math'],
  },
  {
    t: 'Find the Duplicate Number',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Medium',
    desc: 'An array of n+1 integers in range [1, n] contains exactly one repeated value. Find it without modifying the array and using O(1) extra space.',
    sig: ['index as next-pointer forms a cycle', 'cycle entry is the duplicate'],
    brute: 'Sort, then scan neighbors — modifies the array.',
    bot: 'Constraints forbid modification and hashing; treat values as pointers.',
    pHint: 'nums[i] tells you where to jump next — a linked list is hiding here.',
    oHint: 'Floyd cycle detection over i -> nums[i]; the cycle entrance is the duplicate. Phase 2: reset slow to 0, advance both by 1 until equal.',
    java: ['Floyd cycle', 'phase-2 entry finding'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['forgetting phase 2 — meeting point is not the entrance', 'modifying the array'],
    mins: 30,
    ex: [
      { input: '[1,3,4,2,2]', output: '2' },
      { input: '[3,1,3,4,2]', output: '3' },
    ],
    tags: ['array', 'pointers'],
  },
  {
    t: 'LRU Cache',
    cat: 'Linked List',
    pat: 'HashMap Lookup',
    d: 'Medium',
    desc: 'Design an LRU cache with O(1) get and put, evicting the least-recently-used key when capacity is exceeded.',
    sig: ['recency ordering + instant lookup', 'design question'],
    brute: 'A list plus a map; scan the list to move entries to the front.',
    bot: 'List removal is O(n); a doubly-linked list makes it O(1).',
    pHint: 'You need O(1) removal from the middle of an ordered structure.',
    oHint: 'HashMap from key -> node of a doubly-linked list; on use, unlink and move to head; evict tail.prev on overflow.',
    java: ['doubly-linked list', 'HashMap', 'sentinel head/tail'],
    time: 'O(1) per op',
    space: 'O(capacity)',
    traps: ['forgetting to update recency on get, not just put', 'capacity 1 edge case'],
    mins: 30,
    ex: [
      { input: 'capacity 2: put(1,1), put(2,2), get(1), put(3,3), get(2), put(4,4), get(1), get(3), get(4)', output: '1, -1, -1, 3, 4' },
    ],
    tags: ['design', 'hashmap'],
  },
  {
    t: 'Merge K Sorted Lists',
    cat: 'Linked List',
    pat: 'Heap / Top-K',
    d: 'Hard',
    desc: 'Given k sorted linked lists, merge them into one sorted list.',
    sig: ['k moving fronts, always take the smallest', 'repeated min extraction'],
    brute: 'Merge lists two at a time — O(n·k) total.',
    bot: 'Each two-way merge rescans everything; a heap exposes the global minimum in O(log k).',
    pHint: 'All you ever need is the smallest current head.',
    oHint: 'Push each list head into a min-heap keyed by value; repeatedly pop, attach to the result, push its next.',
    java: ['PriorityQueue with Comparator<ListNode>', 'dummy head'],
    time: 'O(N log k)',
    space: 'O(k)',
    traps: ['comparator on node values', 'null heads in the initial heap'],
    mins: 30,
    ex: [
      { input: '[1->4->5, 1->3->4, 2->6]', output: '1 -> 1 -> 2 -> 3 -> 4 -> 4 -> 5 -> 6' },
    ],
    tags: ['linked list', 'heap'],
  },
  {
    t: 'Reverse Nodes in K-Group',
    cat: 'Linked List',
    pat: 'Fast/Slow Pointers',
    d: 'Hard',
    desc: 'Reverse the nodes of a linked list k at a time and return the modified list. Leave the final partial group as-is.',
    sig: ['group-wise surgery', 'length check per group'],
    brute: 'Copy to arrays and rebuild — loses the pointer-practice point.',
    bot: 'The challenge is pointer management, not data movement.',
    pHint: 'First check the group has k nodes; then reverse exactly that run and reconnect.',
    oHint: 'Track groupPrev; check k nodes exist; reverse the run; connect groupPrev -> new head and new tail -> next group.',
    java: ['iterative segment reversal', 'careful pointer bookkeeping'],
    time: 'O(n)',
    space: 'O(1)',
    traps: ['reversing a partial final group', 'losing the connection to the rest of the list'],
    mins: 35,
    ex: [
      { input: '1 -> 2 -> 3 -> 4 -> 5, k = 3', output: '3 -> 2 -> 1 -> 4 -> 5' },
    ],
    tags: ['linked list'],
  },
]
