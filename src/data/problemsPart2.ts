import type { CompactProblem } from './problemHelpers'

/** Phase 7 — Trees (complete). */
export const PROBLEMS_TREES: CompactProblem[] = [
  {
    t: 'Maximum Depth of Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'Return the maximum depth of a binary tree — the number of nodes along the longest root-to-leaf path.',
    sig: ['tree recursion', 'combine children results'],
    brute: 'BFS level counting.',
    bot: 'BFS works but recursion expresses the depth formula in three lines.',
    pHint: 'Depth(node) = 1 + max(depth(left), depth(right)).',
    oHint: 'Recursively: null -> 0, else 1 + max of the two child depths.',
    java: ['recursion base case'],
    time: 'O(n)',
    space: 'O(h) recursion',
    traps: ['forgetting the null base case', 'counting edges instead of nodes'],
    mins: 15,
    ex: [
      { input: '[3,9,20,null,null,15,7]', output: '3' },
      { input: '[1,null,2]', output: '2' },
    ],
    tags: ['tree', 'recursion'],
  },
  {
    t: 'Same Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'Given two tree roots, return true if they are structurally identical with equal node values.',
    sig: ['two-tree recursion', 'compare node plus both children'],
    brute: 'Serialize both trees and compare strings.',
    bot: 'Serialization allocates extra space; direct recursion compares in place.',
    pHint: 'Compare current nodes, then recurse on both children pairs.',
    oHint: 'Both null -> true; one null or values differ -> false; else recurse on left pair and right pair.',
    java: ['recursion with null checks'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['missing the one-null cases', 'comparing values before null checks'],
    mins: 15,
    ex: [
      { input: 'p = [1,2,3], q = [1,2,3]', output: 'true' },
      { input: 'p = [1,2], q = [1,null,2]', output: 'false' },
    ],
    tags: ['tree'],
  },
  {
    t: 'Invert Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'Mirror a binary tree: swap the left and right children of every node, then return the root.',
    sig: ['swap at every node', 'tree recursion'],
    brute: 'Level order, swapping children in a queue.',
    bot: 'Queue machinery is unnecessary — recursion expresses the swap directly.',
    pHint: 'Swap at the current node, then recurse on both subtrees.',
    oHint: 'Recurse to invert both subtrees, then swap the current node\'s two children and return it.',
    java: ['recursion', 'reference swap'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['returning a child instead of the root', 'forgetting either subtree'],
    mins: 15,
    ex: [{ input: '[4,2,7,1,3,6,9]', output: '[4,7,2,9,6,3,1]' }],
    tags: ['tree'],
  },
  {
    t: 'Binary Tree Maximum Path Sum',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Hard',
    desc: 'A path may start and end at any nodes along parent-child links. Return the maximum sum of node values over all paths.',
    sig: ['return one-sided gain, update a global answer', 'negative subtrees can be skipped'],
    brute: 'For each node compute the best downward path through it — O(n²).',
    bot: 'Downward gains are recomputed; one post-order pass updates the answer while returning gains.',
    pHint: 'A path through a node = left gain + node value + right gain.',
    oHint: 'Post-order: gain = val + max(0, leftGain, rightGain); answer = max(answer, val + max(0, leftGain) + max(0, rightGain)).',
    java: ['a one-element array as a mutable maximum'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['returning the through-node sum instead of the one-sided gain', 'not allowing the path to skip negative subtrees'],
    mins: 35,
    ex: [
      { input: '[1,2,3]', output: '6' },
      { input: '[-10,9,20,null,null,15,7]', output: '42', explanation: '15 -> 20 -> 7.' },
    ],
    tags: ['tree', 'recursion'],
  },
  {
    t: 'Diameter of Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'Return the number of edges on the longest path between any two nodes. The path need not pass through the root.',
    sig: ['path through each node', 'post-order accumulation'],
    brute: 'For each node compute depths of both sides — O(n²).',
    bot: 'Depths are recomputed; one post-order pass returns depth and updates the diameter.',
    pHint: 'The longest path through a node is leftDepth + rightDepth edges.',
    oHint: 'DFS returns depth; at each node update diameter with left + right; return 1 + max(left, right).',
    java: ['mutable holder for the running maximum'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['edges vs nodes off by one', 'returning the diameter instead of the depth from the recursion'],
    mins: 20,
    ex: [
      { input: '[1,2,3,4,5]', output: '3', explanation: 'Path 4-2-1-3.' },
      { input: '[1,2]', output: '1' },
    ],
    tags: ['tree'],
  },
  {
    t: 'Balanced Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'A tree is balanced if every node\'s two subtree heights differ by at most one. Return true for a balanced tree.',
    sig: ['height difference check at every node', 'early exit on failure'],
    brute: 'At each node compute both subtree heights separately — O(n²).',
    bot: 'Heights are recomputed; one bottom-up pass returns height and validity together.',
    pHint: 'Return height, but signal invalidity upward when any subtree is unbalanced.',
    oHint: 'DFS returns -1 when unbalanced, else height; check |left - right| <= 1 at each node.',
    java: ['sentinel return value (-1)'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['only checking the root\'s own balance', 'treating null as height 1 instead of 0'],
    mins: 20,
    ex: [
      { input: '[3,9,20,null,null,15,7]', output: 'true' },
      { input: '[1,2,2,3,3,null,null,4,4]', output: 'false' },
    ],
    tags: ['tree'],
  },
  {
    t: 'Subtree of Another Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Easy',
    desc: 'Return true if subRoot is a subtree of root — an exact node-and-value match somewhere inside root.',
    sig: ['same-tree check applied at every node'],
    brute: 'For each node of root, run sameTree against subRoot.',
    bot: 'That is the intended O(n·m) solution; serialization is an alternative.',
    pHint: 'sameTree(root, subRoot), OR the same check at any descendant.',
    oHint: 'isSubtree = sameTree(root, subRoot) || isSubtree(root.left) || isSubtree(root.right).',
    java: ['recursion composition'],
    time: 'O(n·m)',
    space: 'O(h)',
    traps: ['stopping at the first value match without a full comparison', 'null handling inside both recursions'],
    mins: 20,
    ex: [
      { input: 'root [3,4,5,1,2], sub [4,1,2]', output: 'true' },
      { input: 'root [3,4,5,1,2,null,null,null,null,1], sub [4,1,2]', output: 'false' },
    ],
    tags: ['tree'],
  },
  {
    t: 'Lowest Common Ancestor of a BST',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Medium',
    desc: 'Given a BST and two nodes p and q, return their lowest common ancestor node.',
    sig: ['BST ordering decides left/right without searching both sides'],
    brute: 'Find both paths from the root and compare them.',
    bot: 'Storing paths costs O(h) memory; BST order decides each branch instantly.',
    pHint: 'If both targets are smaller go left; both larger go right; otherwise this node is the split point.',
    oHint: 'Iteratively: both < node -> node = node.left; both > node -> node = node.right; else return node.',
    java: ['iterative walk on a BST'],
    time: 'O(h)',
    space: 'O(1)',
    traps: ['forgetting p or q may itself be the ancestor', 'strict vs non-strict comparisons'],
    mins: 20,
    ex: [
      { input: '[6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8', output: '6' },
      { input: 'same tree, p = 2, q = 4', output: '4' },
    ],
    tags: ['tree', 'bst'],
  },
  {
    t: 'Binary Tree Level Order Traversal',
    cat: 'Trees',
    pat: 'BFS',
    d: 'Medium',
    desc: 'Return node values level by level, e.g. [[3],[9,20],[15,7]].',
    sig: ['level-by-level processing', 'queue with a per-level size snapshot'],
    brute: 'DFS with a depth parameter appending to per-level lists.',
    bot: 'DFS works; BFS with a size snapshot is the canonical level pattern.',
    pHint: 'Process the queue in batches of its current size.',
    oHint: 'BFS: levelSize = queue.size(); poll exactly that many; collect values; enqueue children.',
    java: ['ArrayDeque as queue', 'size snapshot per level'],
    time: 'O(n)',
    space: 'O(w) — max width',
    traps: ['polling while the level size changes', 'forgetting null child checks'],
    mins: 20,
    ex: [{ input: '[3,9,20,null,null,15,7]', output: '[[3],[9,20],[15,7]]' }],
    tags: ['tree', 'bfs'],
  },
  {
    t: 'Binary Tree Right Side View',
    cat: 'Trees',
    pat: 'BFS',
    d: 'Medium',
    desc: 'Return the values of the last node on each level, as seen from the right.',
    sig: ['last element of each level'],
    brute: 'Full level order, take the last of each level.',
    bot: 'Full BFS is fine; right-first DFS records one node per depth with O(h) space.',
    pHint: 'One node per level — capture the last of each batch.',
    oHint: 'BFS per level, adding the final polled value; or DFS visiting right before left, recording the first node seen at each depth.',
    java: ['BFS batching', 'depth-keyed recording'],
    time: 'O(n)',
    space: 'O(w)',
    traps: ['assuming the rightmost child is always last on the level', 'empty tree'],
    mins: 20,
    ex: [{ input: '[1,2,3,null,5,null,4]', output: '[1,3,4]' }],
    tags: ['tree', 'bfs'],
  },
  {
    t: 'Count Good Nodes in Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Medium',
    desc: 'A node is good if no node on the root-to-node path has a greater value. Count the good nodes.',
    sig: ['thread a running maximum down the recursion'],
    brute: 'For each node re-scan its path for the maximum.',
    bot: 'Path maxima are recomputed; pass the max down as a parameter.',
    pHint: 'Carry the max value seen from the root into each recursive call.',
    oHint: 'DFS(node, maxSoFar): node is good when node.val >= maxSoFar; update max and recurse both children; sum the results.',
    java: ['passing state through parameters'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['comparing against the parent instead of the whole path max', 'starting maxSoFar too high or too low'],
    mins: 20,
    ex: [{ input: '[3,1,4,3,null,1,5]', output: '4' }],
    tags: ['tree'],
  },
  {
    t: 'Validate Binary Search Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Medium',
    desc: 'Return true if the tree is a valid BST: each node must exceed every node in its left subtree and be less than every node in its right subtree.',
    sig: ['validity ranges tighten down the tree'],
    brute: 'In-order traversal must be strictly increasing.',
    bot: 'In-order needs global state; the range method keeps everything local.',
    pHint: 'Each node inherits a (low, high) window from its ancestors.',
    oHint: 'DFS(node, low, high): node.val must lie inside the window; recurse with (low, node.val) and (node.val, high).',
    java: ['long sentinels for ±infinity bounds'],
    time: 'O(n)',
    space: 'O(h)',
    traps: ['checking only immediate children', 'int overflow at boundaries — use long sentinels'],
    mins: 20,
    ex: [
      { input: '[2,1,3]', output: 'true' },
      { input: '[5,1,4,null,null,3,6]', output: 'false', explanation: '3 sits in the right subtree of 5 but is smaller.' },
    ],
    tags: ['tree', 'bst'],
  },
  {
    t: 'Kth Smallest Element in a BST',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Medium',
    desc: 'Return the k-th smallest value in a BST (1-indexed). An O(h + k) solution exists.',
    sig: ['in-order = sorted order', 'early stop'],
    brute: 'In-order traversal collecting all values, then take index k-1.',
    bot: 'Collecting everything is wasted when k is small; stop after k visits.',
    pHint: 'In-order traversal visits values in ascending order — stop when you have k.',
    oHint: 'Iterative stack in-order: push lefts, pop, decrement k, return when k hits 0, then move right.',
    java: ['iterative in-order with ArrayDeque', 'counter early exit'],
    time: 'O(h + k)',
    space: 'O(h)',
    traps: ['traversing everything instead of stopping early', 'pre-order by mistake'],
    mins: 20,
    ex: [{ input: '[5,3,6,2,4,null,null,1], k = 3', output: '3' }],
    tags: ['tree', 'bst'],
  },
  {
    t: 'Construct Binary Tree from Preorder and Inorder Traversal',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Medium',
    desc: 'Given preorder and inorder arrays of a tree with unique values, rebuild the tree.',
    sig: ['preorder[0] splits inorder', 'index maps avoid rescanning'],
    brute: 'At each step, scan inorder for the root value — O(n²) worst case.',
    bot: 'The split point can be looked up in O(1) with a value -> index map.',
    pHint: 'The first preorder element is always the root of the current range.',
    oHint: 'Map inorder value -> index; recursive build consuming preorder with (lo, hi) inorder ranges.',
    java: ['HashMap index map', 'recursive ranges'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['off-by-one between preorder length and inorder ranges', 'rebuilding the map at every call'],
    mins: 30,
    ex: [
      { input: 'preorder [3,9,20,15,7], inorder [9,3,15,20,7]', output: '[3,9,20,null,null,15,7]' },
    ],
    tags: ['tree', 'recursion'],
  },
  {
    t: 'Serialize and Deserialize Binary Tree',
    cat: 'Trees',
    pat: 'DFS',
    d: 'Hard',
    desc: 'Design encode and decode methods that turn a tree into a string and rebuild it exactly.',
    sig: ['record structure with markers for nulls', 'token stream reconstruction'],
    brute: 'BFS serialization with explicit nulls.',
    bot: 'BFS is acceptable; preorder DFS with null markers gives the shortest correct code.',
    pHint: 'Preorder with explicit null markers rebuilds a unique tree.',
    oHint: 'Serialize preorder with "N" for null; deserialize with a token iterator consuming markers in order.',
    java: ['StringBuilder', 'ArrayDeque as a token queue'],
    time: 'O(n)',
    space: 'O(n)',
    traps: ['missing null markers make the shape ambiguous', 'token order mismatch between the two passes'],
    mins: 35,
    ex: [{ input: '[1,2,3,null,null,4,5]', output: 'round-trips to the same tree' }],
    tags: ['tree', 'design'],
  },
]
